package repository

// This package will hold data-access implementations (Postgres) as API domains migrate from Next.js.
// Define small interfaces next to each domain (e.g. UserReader) to keep tests and swaps manageable.
